Caddy stores its certificate and state under `caddy/data`.

Do not delete this directory after issuing production certificates.

Caddy automatically obtains HTTPS certificates for public DNS names when ports 80 and 443 are reachable and DNS is correct.
